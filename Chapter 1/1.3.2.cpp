/* Commenting out blocks of code is a common practice during debugging, 
 * As the code may include comment pairs, and these don't stack, the best
 * way to comment out a block of code is to comment out each line using a single-line comment "//"
 * as this will work without issue. 
 */
// /*
//  * everything inside a single-line comment is ignored
//  * including nested comment pairs. 
//  */